import styled from 'styled-components'

export const StyledMap = styled.div`
  & .mapboxgl-canvas {
    height: 100vh;
    position: relative;
  }
  & .mapboxgl-ctrl-attrib {
    display: none;
  }
`
